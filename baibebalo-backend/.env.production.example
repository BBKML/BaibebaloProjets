# ══════════════════════════════════════════════════════════════
# BAIBEBALO API - Variables pour PRODUCTION (serveur hébergé)
# ══════════════════════════════════════════════════════════════
# Copiez ce fichier en .env sur le serveur et remplissez les valeurs.
# Ne commitez jamais le fichier .env réel.
# ══════════════════════════════════════════════════════════════

NODE_ENV=production
PORT=5000

# URL publique de l'API (sans slash final) - ex: https://api.baibebalo.ci
API_BASE_URL=https://api.baibebalo.ci

# Base de données PostgreSQL (sur le serveur ou service managé)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=baibebalo
DB_USER=postgres
DB_PASSWORD=VOTRE_MOT_DE_PASSE_FORT

# JWT - Générez des clés longues et aléatoires (ex: openssl rand -base64 64)
JWT_SECRET=VOTRE_JWT_SECRET_TRES_LONG
JWT_REFRESH_SECRET=VOTRE_REFRESH_SECRET_TRES_LONG
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# CORS - Autoriser l'admin et les apps (séparer par des virgules)
# En phase test avec amis vous pouvez mettre * (à restreindre ensuite)
CORS_ORIGIN=https://admin.baibebalo.ci
# Ou pour tout autoriser temporairement :
# CORS_ORIGIN=*

# URLs des frontends (optionnel, pour emails / liens)
CLIENT_APP_URL=https://expo.dev
ADMIN_PANEL_URL=https://admin.baibebalo.ci

# SMS / Firebase / Paiements : à configurer selon votre .env.example complet
